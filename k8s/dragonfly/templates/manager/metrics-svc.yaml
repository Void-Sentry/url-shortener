---
# Source: dragonfly/templates/manager/metrics-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: dragonfly-manager-metrics
  namespace: "dragonfly-system"
  labels:
    app: dragonfly
    chart: dragonfly-1.3.19
    release: dragonfly
    heritage: Helm
    component: manager-metrics
spec:
  type: ClusterIP
  ports:
  - port: 8000
    name: http-metrics
    appProtocol: http
    protocol: TCP
    targetPort: 8000
  selector:
    app: dragonfly
    release: dragonfly
    component: manager
