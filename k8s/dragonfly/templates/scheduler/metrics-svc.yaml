---
# Source: dragonfly/templates/scheduler/metrics-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: dragonfly-scheduler-metrics
  namespace: "dragonfly-system"
  labels:
    app: dragonfly
    chart: dragonfly-1.3.19
    release: dragonfly
    heritage: Helm
    component: scheduler-metrics
spec:
  type: ClusterIP
  ports:
  - port: 8000
    name: http-metrics
    appProtocol: http
    protocol: TCP
    targetPort: 8000
  selector:
    app: dragonfly
    component: scheduler
    release: dragonfly
