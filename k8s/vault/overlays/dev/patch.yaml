# overlays/dev/patch-server-dev.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vault
spec:
  template:
    spec:
      containers:
      - name: vault
        args:
        - "server"
        - "-dev"
